# Candidate /etc/systemd/logind.conf.d/60-macserver.conf
# Apply only after lid-closed cooling and local recovery are qualified.
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
IdleAction=ignore
